// const text = "Hello NodeJS from data.js";

// module.exports = text;

const value = 100;

function sum(a, b) {
  return a + b;
}

function multyply(a, b) {
  return a * b + value;
}

export { sum, multyply };
